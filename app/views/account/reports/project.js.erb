$('.modal .modal-form').html('<%= j render('account/reports/project_report', project: @project)%>');
$('.modal').addClass('is-active');

var project_report = $('.modal .modal-form .project-report .project-doughnut');
var user_progress_report = $('.modal .modal-form .project-report .user-horizontal-bar');

chart(
  project_report.data('graph'),
  project_report[0]
);

horizontalBar(
  user_progress_report.data('graph'),
  user_progress_report[0]
);

