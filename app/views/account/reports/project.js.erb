var modal = document.querySelector('.modal');

modal.querySelector('.modal-form').innerHTML = '<%= j render('account/reports/project_report', project: @project)%>';
modal.classList.add('is-active');

var project_report = modal.querySelector('.modal-form .project-report .project-doughnut');
var user_progress_report = modal.querySelector('.modal-form .project-report .user-horizontal-bar');

chart(
  JSON.parse(project_report.getAttribute('data-graph')),
  project_report
);

horizontalBar(user_progress_report);

