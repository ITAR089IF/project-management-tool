$('.notification').remove();
<% if @result %>
  $('#assignee_form').html('<%=j render "account/tasks/assignee_show", task: @task %>')
<% else %>
  <% flash[:alert] = @task.errors.full_messages.to_sentence %>
<% end %>
