$('#all_tasks').html('');
<%- @all_tasks.each do |task| %>
  task_html = $("<%= j render('/account/projects/show_tasks', task: task, project: @project) %>");
  $('#all_tasks').append(task_html);
<% end %>
